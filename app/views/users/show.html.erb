<div class="container">
    <div class="row">
      <div class="col-md-3">
        <%= render 'users/list' %>
        <%= render 'books/list', book:@book, user:@user %>
      </div>

      <div class="col-md-8 offset-md-1">
        <h2>Books</h2>
        <%= render 'users/table', book:@books, user:@user %>
      </div>
    </div>
</div>