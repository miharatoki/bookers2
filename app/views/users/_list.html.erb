<h2>User info</h2>
<%= attachment_image_tag current_user, :profile_image_id, :fill, 60, 60, format: 'jpeg', fallback: "no_image.jpg" %>
<table>
  <tbody>

    <tr></tr>
    <tr>
      <th>name</th>
      <th><%= current_user.name %></th>
    </tr>
    <tr>
      <th>introduction</th>
      <th><%= current_user.introduction %></th>
    </tr>
  </tbody>
</table>
<%= link_to "ユーザ編集",edit_user_path(current_user.id) %>